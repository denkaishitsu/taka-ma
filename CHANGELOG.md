# Changelog

本プロジェクトの利用者向け変更履歴。形式は [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/)、バージョンは [Semantic Versioning](https://semver.org/lang/ja/) に従う。

## [Unreleased]

## [0.10.0] - 2026-09-03

### Added

- 生成依頼（ファイル作成等）の完了検査に「実在＋最小サイズ」を追加。空ファイルやスタブを「完了」と報告しない。あわせて、生成系モデルを登録する際はその出力を測る検査語彙を同時に追加する規約を設けた
- スレッド会話の明示エスケープ「@taka-ma go」を追加。スレッド内でメンション本文を逐語 `go` とだけ書くと、LLM 判定を待たず直近会話を要約して着手確認へ進む（スラッシュコマンドはスレッドを対象にできないため、その穴を塞ぐ経路）
- ローカル LLM の換装判断を数字で行うための計測を追加。分解の呼び出し単位の成否記録と、失敗率（分解フォールバック率・契約化の縮退率）を算出する集計スクリプト（LLM 不使用）を備え、発動基準を超えたら換装を提案する
- 会話脳がプロンプト内の例文をそのまま返信する誤出力（例文エコー）の機械検出を追加。検出時は応答を棄却して言い直しを求め、換装判断の実測記録に残す
- worker への指示文へ既定裁量の定型を機械付与。生成・編集依頼で worker が細部（内容・文言等）を質問だけ返して止まり、成果物ゼロで終わる逸脱を是正（前提の欠落時は従来どおり作業せず理由を報告し、不在の対象を創作しない）

### Changed

- 承認却下の粒度を操作単位へ是正。保留承認の却下はタスク全体を失敗させず、却下された操作だけを機械拒否して残りの作業を継続する。却下済みの同一操作は再依頼せず拒否で確定する（同じ操作を人へ二度聞かない）
- 読み取り専用ツール（Read・Grep 等の固定小リスト）は LLM 判定を呼ばず即時許可。読み取り操作まで人間承認へ上がる承認疲れを是正
- qu-e 審査の可用性を構造で改善: 審査モデルを常駐化してコールドロードを排除し、タイムアウトは 1 回だけ再試行、それでも応答が無い場合の人間確認は「審査不能」と表示（「危険と判定された」と誤読させない）
- worker 出力の読取上限を 64KB から 10MB へ拡大（設定管理）。行長超過等の読取エラーはインフラ起因として記録し、モデルの「難所」と誤解釈した偽の昇格を起こさない。読取側の異常時は worker プロセスを確実に回収する
- 同一タスクからの監査アラートは 60 秒窓で 1 通のダイジェスト（件数＋パス列挙＋代表理由）に集約し、一時成果物（.tmp 系）は既定で監査対象外に。本当に見るべき承認が連投に埋もれる状態を是正
- 契約の「残り作業の要約」を必須項目化し、タスク分解の入力をこれへ一本化。会話脳の言い換えが実行系へ入る最後の経路を遮断（着手確認には残り作業が常に表示され、見えない縮約は承認されない）
- 「〜は行わない」型の禁止指示は、発話・確定要約のどちらに現れても必ず契約の拘束条件へ拾い、ツール実行前の機械ブロックに載せる運用を既定化

### Fixed

- 「編集してコミット」型の依頼で、コミット・push 等の成果系 git 操作が「済み」と誤判定されて計画から欠落し、完了検査が未達になる問題を修正。残り作業がある場合、成果系操作は編集の後ろへ直列化し、計画時の済み判定を適用しない
- 「@taka-ma go」等の明示エスケープ経由の契約化が、現在ターンの発話（逐語 `go` のみ）を引用できず構造的に必ず不成立になる干渉を修正。明示エスケープに限り現在ターン引用規則を免除する（発話の逐語照合は維持）

## [0.9.0] - 2026-08-28

### Added

- git の定型操作（ブランチ作成・コミット・push・マージ等）を LLM の裁量から外し、決定的な手順（runbook）として実行するレーンを追加。実行された操作は命令と機械照合され、中断後の再開時は実施済みの手順を実測で除外して残りだけを続行する
- 完了した依頼の帰結を終端記録として保持し、状態質問（「終わった？」「push された？」等）には回答時に再検査した実測結果で答えるようになった
- 完了条件の語彙に「ブランチのマージ到達」を追加

### Changed

- 進行・完了などの状態に関する応答を、LLM の記憶による主張から git・タスク台帳の機械確認による実測回答へ全面置換
- 依頼理解の構造化（命令原文・拘束条件・完了条件等の契約抽出）を、構造化出力が実測で不安定だった会話脳からローカル分解脳へ移管。失敗時は上位モデルへ段階的に引き上げる昇格ラダーで補う
- runbook を持つ依頼のタスク分解は git 操作を除いた残り作業の要約のみを入力とし、同じ git 操作が二重実行される構造を撤去

### Fixed

- 取り消し（revert）依頼が元タスクの作業場所を引き継がず、空の作業場で実行される問題を修正

## [0.8.0] - 2026-08-26

### Added

- ローカル LLM の自動監視・入替提案パイプライン（model_watch）を追加。新モデルの公開情報を定期調査し、適合評価（公式ベンチの直接比較・常駐メモリの最小余裕判定）を無人で行って入替候補を提案する。採用の判断と適用は人の承認を経て行う

### Changed

- タスク分解・分類・リスク判定に使うローカル LLM（ya-ta 分解脳）を qwen3.6:27b から qwen3.8:27b へ換装。公式ベンチ全項目で優位・応答速度は同等であることを実測で確認して採用（32K コンテキストでの常駐メモリは実測 18GB）

## [0.7.0] - 2026-08-26

### Added

- 承認した量の拘束条件（「1 行だけ追記」等）を完了時に実測で照合する検査を追加。実際の変更行数（git の実測値）が指定を超えた場合は完了とせず「未達」として報告する。依頼文からの量指定の抽出漏れは決定的検出で補完する
- 作業環境を改変するコマンド（リポジトリの初期化・リモート設定の変更等）を、全タスクで実行前に既定ブロックするようになった。人が承認した逐語命令に含まれる場合のみ許可する。push 失敗時に作業場を勝手に初期化して失敗原因を分からなくする動きを予防する
- 成果物のファイルパスを含む編集系の依頼へ、対象ファイルの完了検査を自動付与するようになった（push 到達の既定検査を成果物編集全般へ拡張。削除系・絶対パス・URL 等は除外）

### Fixed

- ボット ID 形式のメンションがスレッド内で黙殺される問題を修正。あわせて、こちらが返答待ちのスレッドではメンション無しの返信も受理するようになった
- 実際にはタスクが動いていないのに「作業中です」と応答することがある問題を修正。進行報告の前に実行中タスクの実在を機械確認し、無ければ「タスクは走っていません」と実測で返す
- 対象と動作が明確な依頼に不要な細部の確認質問を返して着手しない取りこぼしを、プロンプト規則の明文化と送信前の再判定の 2 層で是正（分離実測で取りこぼし 37%→6%、雑談・質問への誤発火なし）
- 完了検査の対象パスが絶対パスで指定された場合に検査が常に合格扱いになる（または誤って未達になる）問題を修正。作業場所配下の絶対パスは相対へ正規化し、配下でないパスは安全側で不合格とする

## [0.6.0] - 2026-08-25

### Added

- 会話と実行を結ぶ「受け渡し契約」を導入。依頼から抽出した命令の原文・拘束条件・完了条件・作業場所が着手確認に常時表示され、人が承認した値だけが実行と検証に使われる（散文からの推測を実行層から排除）
- 完了条件の実測検査を導入。承認された完了条件（リモートへの push 到達・ファイルの存在・変更の有無など）を完了時に実際のコマンドで検査し、全項目 PASS のときだけ「完了」と報告する。未達の場合は検査の実出力つきで「未達」と報告する
- 依頼は完了条件が満たされるまで閉じない。タスクが終わっても検査が通らなければ「未達」のまま残り、後続の作業で満たされた時点を自動検出して閉じる（宣言では閉じない）
- 実リポジトリでの作業が必要な依頼で作業場所が未解決の場合、実行前に停止して指定を求める（空の使い捨て作業場で走ってから気づく構造を廃止）
- 「git push -u origin ...」のような具体的なコマンド命令は、要約・分解を通さず原文のまま実行される。実行されたコマンドと命令の機械照合が完了報告に併記され、不一致は完了にならない
- 「〜するな」型の拘束条件は承認判定に機械強制として登録され、該当するコマンドはツール実行前にブロックされる
- 失敗が 2 回連続した依頼は自動での再計画を停止し、失敗原因と必要な入力（リポジトリ指定など）を平文で報告する

### Fixed

- AI 応答の JSON 解釈を堅牢化（不正なエスケープ文字の機械修復、応答内にコードブロックが含まれる場合の抽出誤りの解消）
- 発話の処理中に常駐プロセスが再起動すると、そのメッセージが応答されないまま滞留する問題を修正（起動時に回収して再処理）

## [0.5.0] - 2026-08-17

### Added

- Even Realities G2（AR グラス）からの指示・会話チャネルを追加（第1段）。グラスからの発話を Claude リレー経由で Slack の会話へ中継し、応答・承認確認をグラス側へ返す。往復はシェルスクリプト方式（外部サーバ・追加常駐なし）で、上り中継の一本化により 1 ターンの所要を 45 秒から 17 秒へ短縮。構築手順書 09 を新設
- 依頼（会話）とタスクの継続紐づけを追加。確定タスクが由来の会話セッションと恒久的に紐づき、スレッド内の過去の文脈（冒頭の依頼を含む）を参照して回答・分解できるようになった。日を跨いだスレッドでも冒頭に提示済みの内容へ即答する（実機で検証済み）

### Fixed

- Slack 受信時にアプリ帰属表記が本文へ混入し原文を汚染する問題を受信入口で修正
- 単体テストの全量一括実行が収集段階で崩壊する問題を修正（テストが配備スクリプトを誤って読み込む名前衝突の解消。開発環境向け）

### Documentation

- 会話の流れから依頼意図の変化を AI が検出し、人の承認を経て要件へ追記する機構（intent 連続捕捉）の設計を設計書へ追加（実装は今後）

## [0.4.0] - 2026-08-16

### Added

- タスクの完了報告を実際のコマンド実行結果で裏取りするようになった。push の完了はリモート側でコミットを確認できた場合のみ「完了」と報告し、確認できない場合は「未完了」と証跡（終了コード・実出力）付きで報告する
- 作業状態の確認質問（リポジトリ・ブランチ・ファイル名など）に、「確認します」という宣言ではなく実際のコマンド実行結果を 1 メッセージで即答するようになった
- 「中止します」等の停止指示を、承認ボタンを挟まず即時実行するようになった。提示中の計画・実行中/待機中のタスクをその場で停止し、停止した項目の一覧だけを返す
- 作業対象リポジトリを自然な文章（「リポジトリ: /path の…」等）でも指定できるようになった。指定は会話に持続し（冒頭で指定して後から着手しても有効）、`~/` 始まりのパスも展開される。着手確認に作業場所（workspace）が常時表示され、未指定の場合は「未指定（既定の空作業場）」と明示される
- worker 起動前に SSH・git リモート・AI 認証の事前検査（プリフライト）を追加。認証切れ等は作業を始める前に原因種別つきで通知され、種別の取り違え（SSH の問題を AI 認証の問題と誤報するなど）が起きない
- 配備時に、未マージのブランチからの配備を自動停止するガードを追加。マージ済みの修正が古い作業コピーの配備で巻き戻る事故を防ぐ（意図的な迂回は明示フラグでのみ可能）

### Changed

- 依頼を処理する AI モデルを最新世代へ更新。重い作業は Claude Opus 5、中量は Claude Sonnet 5、軽量は Claude Haiku 4.5 が担当する。最難関の作業向けに Claude Fable 5 を明示指定できる点は従来どおり
- Gemini 側も最新世代へ更新。既定を Gemini 3.6 Flash とし、最上位の Gemini 3.1 Pro を明示指定できるようにした。従来「指定しても既定に戻ってしまう」状態だった上位モデルの指定が、実際に切り替わるようになった

### Fixed

- 明確な単発依頼（「◯◯の README を要約して」等）に「やりますね」と応答したまま実行へ進まない問題を修正。対象と動作が明確な依頼は一息で実行計画の提示まで進み、実行を宣言する応答が会話継続のまま止まることはなくなった（雑談・質問への誤発火は実測で退行なし）
- 会話応答の内部データ（制御用 JSON）が壊れた応答経由で生のまま返信に混じることがある問題を修正。人向けの言い直し文言に縮退する
- 会話応答が契約形式（ready の欠落・型不正）から外れた場合の扱いを明示化し、安全側（会話継続）に倒した上で発生を記録して観測できるようにした
- 上位 Gemini モデルを指定しても反映されなかった問題を修正。原因は CLI へ渡す指定の書き方（並び順とモデル名の表記）にあり、CLI 側の不具合ではなかった
- 実体のなくなった旧世代モデルの登録を削除。指定できるのに実際には別のモデルが動く、という食い違いを解消した

### Documentation

- 再発防止の実機テストシナリオ集（E2E 仕様 T09）を追加。虚偽完了の検出・実測応答・停止即実行・リポジトリ指定の明示などをリリース前リグレッションとして定義
- 構築手順書へ配備ガードと新設定項目（プリフライト・作業ホームパス）を追記。運用ドキュメントのリポジトリ指定の説明を現行機能（自然文指定・持続・作業場所の常時表示）に追従
- 設計書・構築手順書・運用ドキュメント・E2E 仕様のモデル名を最新世代へ統一
- モデル指定が黙って既定へ落ちる場合があるため、指定が実際に効いたかを確認する手順を記載。応答文の自己申告ではなくログで判定する

## [0.3.0] - 2026-07-23

### Added

- 実行前に「実行計画」を提示して承認を取るゲートを追加。段組み（どの作業を並行し、どこで待ち合わせるか）と各作業の担当モデル・深さが着手前に分かる。承認した計画はそのまま実行され、提示内容と実際に走る内容がずれない
- 提示された計画を、承認前にチャットで訂正できる。簡易な指定（担当モデルや深さの変更）は即座に反映して計画全体を再提示し、自然な文章での指示は変更点だけを差分で確認できる。訂正はスレッド内・スレッド外のどちらの投稿でも受け付ける

### Changed

- 依頼の振り分けを「軽い/重い」の2値から、実行形態（その場で答える／エージェントとして作業する）と深さ（浅い／深い）の2軸に再設計。判定はどのモデルを使うかを直接決めず、設定ファイルの対応表が担当モデルを決めるため、モデル構成の変更が設定編集だけで済む
- 実行が失敗・タイムアウトした場合、より能力の高いモデルへ段階的に引き上げて再試行するようになった。作業側からの「手に負えない」という申告も引き上げの引き金になる
- その場で答えるだけの依頼と、作業を伴う依頼を別レーンで処理するよう変更。片方の混雑がもう片方を待たせない

### Documentation

- 設計書を上記の振り分け2軸化・計画確認ゲートに合わせて改訂
- 構築手順書（AI Gateway）を振り分け2軸の設定手順へ更新
- 構築手順書（Sentinel）へ、終了済み作業ディレクトリの自動整理（削除対象と除外条件・確認手段・起動時と日次の実行タイミング）を追記
- システム俯瞰へ「リリースノート（予定）」として今後の投入順とマイルストーンを追加

## [0.2.1] - 2026-07-19

### Added

- タスクごとの作業ディレクトリ（workspace）を、そのタスクが完了したら自動で退避・整理する仕組みを追加。使い終わった作業領域が溜まり続けない。対象や保持数は設定ファイル（yaml）で指定する
- チャット受信（常時接続）の死活監視を追加。接続が無応答になっても内部で検知して復帰でき、「メッセージを送ったのに反応しない」状態が続くのを防ぐ

## [0.2.0] - 2026-07-17

### Added

- コミット前に変更内容を自動監査し、問題を検出したらコミットを止めるゲートを追加。監視対象のリポジトリを設定で指定でき、実際の開発リポジトリの変更を動的に監視する

### Changed

- 運用パラメータの既定値をコード内から排除し、設定ファイル（yaml）を単一の正本に統一。挙動を変えたいときはコードではなく設定を編集する
- 会話・タスク分解のローカル LLM 呼び出しを `ollama run` の都度起動から HTTP API（/api/generate）へ変更。モデル常駐（keep_alive）と KV キャッシュ再利用により、会話が長くなるほど毎ターン遅くなる問題を解消
- タスク完了通知の切り詰め（先頭 500 文字のみ）を廃止し、実行結果の全文を受け取れるように変更
- 実行結果を会話文脈へ還流するよう変更。タスク完了後に「さっきの結果はどこ」等の後続質問へ答えられる
- 会話履歴をディスクへ永続化。プロセス再起動や時間経過で会話の文脈が失われない

### Fixed

- 着手確認が一定時間で自動タイムアウトし「もう一度指示してください」を強いる挙動を撤廃。人間がボタンで決着するまで待ち続ける
- LLM 呼び出し失敗が一律「内部エラー」としか通知されなかった問題を修正。タイムアウトと接続不可を区別して原因を通知する
